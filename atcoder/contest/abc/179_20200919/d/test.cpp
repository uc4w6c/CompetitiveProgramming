#include <bits/stdc++.h>
using namespace std;

// 
int main() {
    set<int> test;
    test.insert(5);
    test.insert(4);
    test.insert(6);
    for (auto a : test) {
        cout << a << endl;
    }

    int ans = 998244353;
    cout << ans;
}
