#include <bits/stdc++.h>
using namespace std;

// 

int main() {
    int N;
    cin >> N;

    int X[N][2];
    int x, y;
    for (int i = 0; i < N; i++) {
        cin >> x >> y;
        X[i][0] = x;
        X[i][1] = y;
    }

    for (int i = 0; i < N - 2; i++) {
        for (int j = 0; j < N - 1; j++) {
            
            for (int k = 0; k < N; k++) {
            }
        }
    }
    cout << "No";
    return 0;
}
