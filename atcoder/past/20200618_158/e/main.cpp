#include <bits/stdc++.h>
using namespace std;

// E - Divisible Substring
// まずはTLEしそうなパターン
int main() {
    int N, S, P;
    cin >> N >> S >> P;

    for (int i = P; i <= S; i += P) {
        // i の桁数を取得してそれずつズラす
    }

}
