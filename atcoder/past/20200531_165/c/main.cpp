#include <bits/stdc++.h>
using namespace std;

// C - Many Requirements

// 出来ていない
int main() {
    int N, M, Q;
    cin >> N >> M >> Q;

    int a[Q], b[Q], c[Q], d[Q];

    for (int i = 0; i < Q; i++) {
        cin >> a[i];
        cin >> b[i];
        cin >> c[i];
        cin >> d[i];
    }

    int A[N];

    long long sum = 0;




    cout << sum;
}
