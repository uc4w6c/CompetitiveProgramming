#include <bits/stdc++.h>
using namespace std;

int main() {
    long long maxA = pow(10, 9);
    long long maxX = pow(10, 18);
    cout << maxA << endl;
    cout << maxX << endl;
    cout << maxA * 10000000000 << endl;
}
